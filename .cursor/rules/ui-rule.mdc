---
description: 
globs: packages/ui/src/**/*.vue,packages/ui/src/**/*.ts
alwaysApply: false
---
# ui 子项目规则

本子项目为前端 UI 相关代码，主要用于实现界面组件和页面。

## 目录结构
- [src/](mdc:packages/ui/src)：主源码目录，包含组件、页面等
- [public/](mdc:packages/ui/public)：静态资源
- [index.html](mdc:packages/ui/index.html)：入口 HTML 文件
- [vite.config.ts](mdc:packages/ui/vite.config.ts)：Vite 配置
- [tsconfig.json](mdc:packages/ui/tsconfig.json)：TypeScript 配置
- 其他配置文件如 `.prettierrc.json`、`tsconfig.*.json`、`env.d.ts` 等

## 命名规范
- 目录、文件名：小写字母+中划线（kebab-case）
- 组件文件建议与组件名保持一致，用大驼峰（PascalCase）
- 变量/函数：小驼峰（camelCase）
- 类/类型/接口：大驼峰（PascalCase）
- 常量：全大写+下划线（SNAKE_CASE）

## 代码风格
- 推荐使用 TypeScript
- 所有函数、类、接口、类型、重要变量需使用 JSDoc 注释，说明用途、参数和返回值
- 文件内导出内容与文件名保持语义一致
